# Tempo Mock Server Configuration

# Server port
PORT=8444

# Must match mock-jira seed for user consistency
DATA_SEED=capacity-planner-mock-2024

# User count (must match mock-jira)
NUM_USERS=500

# Team configuration
NUM_TEAMS=20
MEMBERS_PER_TEAM_MIN=15
MEMBERS_PER_TEAM_MAX=40

# Worklog generation settings (Heavy density)
WORKLOGS_PER_ISSUE_MIN=30
WORKLOGS_PER_ISSUE_MAX=50

# Plan generation settings
PLANS_PER_USER_MIN=2
PLANS_PER_USER_MAX=6

# Date range (must match mock-jira)
START_YEAR=2022
CURRENT_YEAR=2026
